% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta_analysis.R
\name{perform_random_effects_meta}
\alias{perform_random_effects_meta}
\title{Perform Random-Effects Meta-Analysis}
\usage{
perform_random_effects_meta(
  meta_data,
  log_transform = TRUE,
  conf_level = 0.95,
  add_weights = TRUE
)
}
\arguments{
\item{meta_data}{Data frame with columns: study, hr, lower, upper. Can also include
additional columns that will be preserved in the output.}

\item{log_transform}{Whether to log-transform hazard ratios for analysis (default: TRUE)}

\item{conf_level}{Confidence level for intervals (default: 0.95)}

\item{add_weights}{Whether to add weight columns to output data (default: TRUE)}
}
\value{
List with meta-analysis results, including pooled estimate and data with weights
}
\description{
This function performs a random-effects meta-analysis using the DerSimonian and Laird
method on hazard ratios from multiple studies.
}
\examples{
\dontrun{
meta_data <- data.frame(
  study = c("Study 1", "Study 2", "Study 3"),
  hr = c(1.5, 2.1, 1.8),
  lower = c(1.1, 1.6, 1.2),
  upper = c(2.0, 2.8, 2.7)
)

meta_results <- perform_random_effects_meta(meta_data)
print(meta_results$pooled_hr)  # Pooled hazard ratio
}
}
