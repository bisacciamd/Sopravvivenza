% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta_analysis.R
\name{perform_spline_meta_regression}
\alias{perform_spline_meta_regression}
\title{Perform Spline Meta-Regression}
\usage{
perform_spline_meta_regression(
  meta_data,
  moderator_name,
  df = 2,
  log_transform = TRUE,
  conf_level = 0.95,
  method = "REML",
  n_pred = 100
)
}
\arguments{
\item{meta_data}{Data frame with columns: hr, lower, upper, and the moderator variable}

\item{moderator_name}{Name of the moderator variable in meta_data}

\item{df}{Degrees of freedom for spline (default: 2)}

\item{log_transform}{Whether to log-transform hazard ratios for analysis (default: TRUE)}

\item{conf_level}{Confidence level for intervals (default: 0.95)}

\item{method}{Estimation method: "REML" (default), "ML", "DL", or "FE"}

\item{n_pred}{Number of prediction points for smooth curve (default: 100)}
}
\value{
List with meta-regression results, including model, predictions, and specific points
}
\description{
This function performs a spline meta-regression to analyze non-linear relationships
between a moderator variable and effect sizes (e.g., hazard ratios).
}
\examples{
\dontrun{
# Meta-regression with GLS values as moderator
meta_data <- data.frame(
  study = c("Study 1", "Study 2", "Study 3"),
  hr = c(1.5, 2.1, 1.8),
  lower = c(1.1, 1.6, 1.2),
  upper = c(2.0, 2.8, 2.7),
  gls = c(-16, -14, -12)  # Moderator variable
)

spline_results <- perform_spline_meta_regression(
  meta_data, 
  moderator_name = "gls"
)
}
}
