% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{create_funnel_plot}
\alias{create_funnel_plot}
\title{Create Funnel Plot for Publication Bias Assessment}
\usage{
create_funnel_plot(
  meta_results,
  precision_measure = "se",
  title = "Funnel Plot",
  xlab = "Effect Size",
  ylab = NULL,
  filename = "funnel_plot",
  output_dir = NULL,
  file_format = "both",
  show_contours = TRUE,
  show_se_lines = TRUE,
  inverted = ifelse(precision_measure == "se", TRUE, FALSE),
  eggers_line = TRUE
)
}
\arguments{
\item{meta_results}{Results from perform_fixed_effects_meta or perform_random_effects_meta}

\item{precision_measure}{Measure for y-axis: "se" (standard error, default), "precision" (1/se), or "sample" (sample size)}

\item{title}{Plot title (default: "Funnel Plot")}

\item{xlab}{X-axis label (default: "Effect Size")}

\item{ylab}{Y-axis label (default: NULL, auto-generated)}

\item{filename}{Output file name without extension (default: "funnel_plot")}

\item{output_dir}{Output directory (default: NULL, no file saved)}

\item{file_format}{File format for saving: "pdf", "png", or "both" (default: "both")}

\item{show_contours}{Whether to show contour lines for significance (default: TRUE)}

\item{show_se_lines}{Whether to show standard error guidance lines (default: TRUE)}

\item{inverted}{Whether to invert the y-axis (default: TRUE for "se", FALSE otherwise)}

\item{eggers_line}{Whether to show Egger's regression line (default: TRUE)}
}
\value{
A list with plot object and, if requested, paths to saved files
}
\description{
This function creates and optionally saves a funnel plot to assess
publication bias in meta-analysis.
}
\examples{
\dontrun{
# Perform meta-analysis
meta_data <- data.frame(
  study = c("Study 1", "Study 2", "Study 3"),
  hr = c(1.5, 2.1, 1.8),
  lower = c(1.1, 1.6, 1.2),
  upper = c(2.0, 2.8, 2.7)
)

meta_results <- perform_fixed_effects_meta(meta_data)

# Create funnel plot
create_funnel_plot(
  meta_results,
  output_dir = "figures"
)
}
}
