% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{extract_median_survival}
\alias{extract_median_survival}
\title{Extract Median Survival Time}
\usage{
extract_median_survival(
  time_points = NULL,
  surv_probs = NULL,
  ipd = NULL,
  time_var = "time",
  status_var = "status",
  interpolate = TRUE
)
}
\arguments{
\item{time_points}{Vector of time points from digitized curve}

\item{surv_probs}{Vector of survival probabilities from digitized curve}

\item{ipd}{Optional individual patient data (if provided, time_points and surv_probs are ignored)}

\item{time_var}{Name of time variable in IPD (default: "time")}

\item{status_var}{Name of status variable in IPD (default: "status")}

\item{interpolate}{Whether to interpolate median survival time (default: TRUE)}
}
\value{
Median survival time
}
\description{
This function extracts the median survival time from digitized curve data
or from reconstructed IPD.
}
\examples{
\dontrun{
# From digitized curve data
time_points <- c(0, 6, 12, 18, 24, 30, 36)
surv_probs <- c(1, 0.9, 0.8, 0.7, 0.5, 0.4, 0.3)
median_survival <- extract_median_survival(time_points, surv_probs)

# From reconstructed IPD
median_survival <- extract_median_survival(ipd = result$ipd)
}
}
